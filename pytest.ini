[pytest]
# Put the project root on sys.path so `import fuzzer` resolves however pytest
# is invoked. Without this, only `python -m pytest` works -- that form adds the
# current directory to sys.path as a side effect of `-m`, while a bare `pytest`
# does not, so the suite passed or failed depending on how you typed it.
pythonpath = .
testpaths = tests
